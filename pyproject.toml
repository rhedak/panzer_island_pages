[project]
name = "panzer-island-pages"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.5",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pyyaml>=6.0",
]
